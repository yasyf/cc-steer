# GENERATED by cc-guides from .claude/fragments/great-docs.yml/ — do not edit; edit the fragments and run 'cc-guides render'.
# Great Docs configuration — https://posit-dev.github.io/great-docs/
# Build: uv run great-docs build · Preview: uv run great-docs preview

module: cc_steer
display_name: cc-steer
dynamic: true
repo: https://github.com/yasyf/cc-steer
site_url: "https://yasyf.github.io/cc-steer/"
pypi: true

navbar_color: "#3a2a10"
accent_color:
  light: "#c62828"
  dark: "#ef6b62"

social_cards:
  image: docs/assets/social-preview.jpg
  twitter_site: "@yasyf"

hero:
  name: cc-steer
  tagline: "Your best training data is rotting in ~/.claude."

cli:
  enabled: true
  module: cc_steer.cli
  name: main

authors:
  - name: Yasyf Mohamedali
    role: Maintainer
    github: yasyf
    email: yasyfm@gmail.com

reference:
  - title: Scanning
    desc: Mine feedback events out of transcripts, incrementally.
    contents:
      - scan.scan
      - scan.ScanReport
  - title: Detection
    desc: The signal detectors and the candidate shape they emit.
    contents:
      - detectors.detect
      - models.FeedbackCandidate
      - models.dedup_key
  - title: Storage
    desc: The feedback event store.
    contents:
      - store.FeedbackStore
  - title: Retraining
    desc: The weekly retrain lanes — gate and watcher — and their promotion machinery.
    contents:
      - retrain.lexical.retrain_gate
      - retrain.watcher.retrain_watcher
      - retrain.watcher.WatcherRecipe
      - retrain.watcher.register_watcher_adapter
      - retrain.watcher.seed_incumbent_probs
      - retrain.evalset.freeze_eval
      - retrain.evalset.freeze_steer_type
      - retrain.evalset.freeze_pick
      - retrain.evalset.EvalFrame
      - retrain.evalset.SteerTypeFrame
      - retrain.evalset.PickFrame
      - retrain.promotion.corrected_gate
      - retrain.promotion.GateResult
      - retrain.promotion.Verdict
  - title: Many-shot watcher
    desc: Score the frozen frame through a byte-constant, cache-amortized many-shot exemplar prefix.
    contents:
      - retrain.manyshot.build_exemplar_system
      - retrain.manyshot.score_frame
  - title: Instrument
    desc: Paired fast-DeLong AUC comparison and the instrument-card decision rule.
    contents:
      - instrument.delong_se
      - instrument.paired_delong
      - instrument.PairedDeLong
      - instrument.bootstrap_ci
      - instrument.mde
      - instrument.actionable
  - title: Decisions
    desc: Mine AskUserQuestion decision rounds from transcript trees into a parquet dataset.
    contents:
      - decisions.mine
      - decisions.DecisionRow
      - decisions.MineResult
      - decisions.Quarantine
      - decisions.write_decisions
      - decisions.read_decisions
      - decisions.stats_of
      - decisions.DecisionStats

# Composed by concatenation after each repo's local fragment: top-level keys
# here must not collide with any repo fragment (last key silently wins).
parser: google
github_style: widget
jupyter: python3
dark_mode_toggle: true
back_to_top: true
keyboard_nav: true
fast_sidebar: true
freeze: auto

# Fleet SEO: uniform page-title format (schema placeholders {page_title}/{site_name}; default
# separator is "|"). social_cards stays per-repo — repos set social_cards.image in their fragment.
seo:
  title_template: "{page_title} - {site_name}"

pre_render:
  - docs/scripts/.gd-build/native_reference_titles.py
