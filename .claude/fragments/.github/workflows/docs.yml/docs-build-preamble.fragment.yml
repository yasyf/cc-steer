name: Documentation

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: docs-${{ github.ref }}
  cancel-in-progress: true

env:
  # ~285-340s real render vs gd-build's 300s default; lift below the 15-min job cap.
  GD_BUILD_TIMEOUT: "780"
