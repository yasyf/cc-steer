{"v":1,"lamport":1,"ops":[{"kind":"create_doc","nonce":"a79a62b2c4254c60e56d23fce9f377ab","title":"Eval refreeze rule (clean-slate era): deliberate, versioned, guarded, journaled","body":"# Eval refreeze rule\n\nThe eval-v3 prereg/accrual apparatus (preregistered freeze dates, addendum counters, accrual targets) is **scrapped** as of 2026-07-16 — see the clean-slate entry in the `EXP eval-v3` log. The frozen eval frame under `~/.cc-steer/eval/` is refreshed by this rule instead:\n\n1. **Refreezes are deliberate and rare.** There is no scheduled refreeze. Refreeze only when the eval frame no longer represents the problem (data-integrity fix, major distribution shift, deliberate scope change) — never as a side effect of a pipeline pass.\n2. **The guards are the gate.** `freeze_eval` refuses to overwrite different content (`FrozenViolationError`) and refuses any frame with empty rendered context (`EmptyEvalContext`). Never delete a frozen file to \"make it work\" mid-task; a refreeze is its own explicitly-decided operation.\n3. **Refreeze procedure**: (a) archive the current `~/.cc-steer/eval/` (move aside, e.g. to Trash with a dated name — never edit in place); (b) re-export from the store (`cc-steer export --push`); (c) `cc-steer freeze-eval` (freezes gate + watcher from the fresh test splits); (d) re-run `freeze-eval` and confirm the idempotent no-op; (e) journal the shas + reason in cc-notes.\n4. **Refreezing invalidates downstream state — handle all of it**: the `probs/` cache is gone with the wipe (incumbents must be re-scored on the new frame before any gate comparison, or the retrain runs `--fresh-epoch`); any human-labeled golden packet built on the old frame is void and must be re-labeled; registry metadata `dataset_digest` values referencing the old frame no longer match `should_retrain`'s digest check, which forces a real retrain on the next pass — expected, not a bug.\n5. **Train/test hygiene is inherited**: the frame is the export's session-hash test split; the only leakage guard needed is the split itself.\n\nCurrent frame (frozen 2026-07-16, clean-slate): gate `c7f923eb…72a5` sha `c7f923eb83d0d850495d080d7951d144ffdb4055d332bb90c09dea1b64972ac5`, watcher sha `69249e2b5525750a752a80d6df0f453079a73c6db48d20c3e82d12092152b07e`; watcher train digest of record `f21c40cac0ad9f88`. Prior (eval-v2-era) frame preserved at `~/.Trash/cc-steer-eval-v2-wiped-20260716`.","when":"Before refreezing the eval frame, running cc-steer freeze-eval, or touching ~/.cc-steer/eval/ in any way","tags":["eval","refreeze","clean-slate"],"anchors":[]}]}