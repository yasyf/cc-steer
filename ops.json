{"v":1,"lamport":8,"session":"4d0c2eb6-0ea0-4b01-a0f7-382e3965c934","ops":[{"kind":"set_criterion_status","id":"75834e1c07dbd331ae328fe06d64311a","status":"met","note":"yclaw migrated to athome serve activator 0.9.2 on this host; commit a4da1c3f19c2 (hunk-scoped — the concurrent vault workstream's uncommitted WIP left untouched in @), CI green. Live verification: probe-no-wake (/health + /v1/models replay the pre-seeded hermes model id, /admin 404s, zero spawns); real completion wake in 9s (warm page cache; 150s budget); idle reap observed at IDLE_S=120 (child exited, probes kept answering), then restored to 1800; bind is tailnet-IP-only (100.88.252.58:8000 via lsof); hermes → metal:8000 answers HTTP 200 (verified via /dev/tcp from root@hermes). Caveat: validate-hardening.sh itself reports FAILs on its hermes-side checks because `tailscale ssh admin@hermes` + curl-on-hermes are broken — pre-existing, orthogonal to the migration (likely the in-flight vault workstream's hermes rebuild); every underlying §3 assertion verified green by hand."}]}